# PneumoScan AI — Python dependencies
# Install with: pip install -r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9      # needed for file uploads

# ML
torch==2.3.0
torchvision==0.18.0
Pillow==10.3.0
numpy==1.26.4
opencv-python-headless==4.9.0.80   # headless = no GUI deps (server safe)

# Data / DB (for patient records later)
sqlalchemy==2.0.30
psycopg[binary]==3.1.19      # Postgres driver (v3) — pre-built wheels, no pg_config needed
pydantic==2.7.1
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0

# Email
resend==2.0.0
python-dotenv==1.0.1 
